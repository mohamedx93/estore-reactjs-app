import React, { ReactElement } from 'react'

interface Props {
    
}

export default function auth({}: Props): ReactElement {
    return (
        <div>
            <h1>Auth</h1>
        </div>
    )
}
